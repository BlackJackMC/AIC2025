# AIC2025

## API endpoints
Please go to `/docs` endpoint to see the documents

## Static files
Everything under `/dataset` folder. 

For example, `GET /dataset/videos/L26_V256_0_20.mp4`

## Dataset folder structure

```
dataset/
├── clip-features: clip vectors only, no nested folder
├── transcript: transcript only, no nested folder
└── segments: video only, no nested folder
```